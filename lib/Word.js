const Letter = require("./Letter");

// Describe 1: Word class is responsible for creating the array of Letter objects.
class Word {
// Since we are guessing a word, we must split the word into a letters array and rejoin below once the word is guessed.


// Describe 2: Check to see if the guessed word is correct or each letter guessed is correctly - return true. If the letter guessed is incorrect then return false.


//Describe 3: 
}

module.exports = Word;